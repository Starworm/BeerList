/** dictionary with the list of API's endpoints */

export class UrlsDictionaries {
  /** root endpoint */
  static API = `https://api.punkapi.com/v2`;
  /** 'Get beers' endpoint */
  static BEER = `${UrlsDictionaries.API}/beers`;
}
